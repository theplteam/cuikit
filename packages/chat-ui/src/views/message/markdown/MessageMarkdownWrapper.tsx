import React from "react";
import Box, { BoxProps } from "@mui/material/Box";

const MessageMarkdownWrapper: React.FC<BoxProps> = (p) => <Box {...p} dir='auto' />

export default MessageMarkdownWrapper;
